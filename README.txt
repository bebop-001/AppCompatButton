This is an example of a custom button that allows
a button to do both short and long click from xml
-- which the standard button doesn't allow.

It does this by extending the standard AppCompatButton
class.

